# The compose file contains runnable PoC defaults. Copy this file to .env to
# override the simulated identities and secrets.
KEYCLOAK_ADMIN_PASSWORD=admin-poc-only
UEM_SESSION_SECRET=local-poc-change-me
